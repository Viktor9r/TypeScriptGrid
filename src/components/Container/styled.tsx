import { styled } from '@mui/material/styles';

export const StyledPageContainer = styled('div')(() => ({
  maxWidth: '1250px',
  margin: 'auto',
  padding: '20px',
  overflow: 'hidden',
}));
